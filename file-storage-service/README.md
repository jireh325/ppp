# file-storage-service
Base package: com.example.file_storage_service
Hexagonal layout generated.
Notes:
- No Eureka / No Spring Cloud Config in this template (use env vars / k8s config + Vault).
- Enable JPA/Kafka per service by editing pom.xml and application.yml.
- Configure Keycloak OIDC issuer in application.yml in production.
